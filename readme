Para Instalar o cypress ou criar um projeto do Zero, lembrar que sempre tem colocar o projeto dentro do cypress:

NPM INSTALL CYPRESS --SAVE -DEV
npm install cypress --save -dev


Para rodar:   NPX CYPRESS RUN